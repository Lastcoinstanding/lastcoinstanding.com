{
  "name": "lastcoinstanding",
  "version": "0.1.0",
  "description": "Last Coin Standing — Bitcoin education through contrast and clarity",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "watch": "eleventy --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5"
  },
  "engines": {
    "node": ">=18"
  }
}
