/* Work With Me — no page behaviour of its own.
   The page's only interactive element is the shared feedback widget, which
   carries its own script inside components/page-feedback.njk. This stub keeps
   the page on the standard _pageassets file pattern (NEW_PAGE_CHECKLIST §2)
   and gives any future behaviour an obvious home. */
